<%= form_for @project do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <br>
  <%= f.check_box :is_private %>
  <br>
  <%= f.submit t('.create_project') %>

<% end %>
